/**
 * crypto/ index — placeholder barrel export.
 *
 * Individual modules (keygen.js, encrypt.js, decrypt.js) will be added
 * in the crypto implementation phase.
 */

// Export nothing yet — file exists so imports don't break during scaffolding.
export {}
